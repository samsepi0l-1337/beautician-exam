# OS
.DS_Store

# Test / tooling artifacts
.playwright-mcp/
*.playwright-mcp
# QA 검증 중 생성되는 루트 스크린샷(사이트 자산 아님)
/*.png

# OMC operational state
.omc/

# Internal build/planning artifact (not used by the site at runtime)
data/plan.json

# Dependencies (none currently, but guard against accidental installs)
node_modules/
